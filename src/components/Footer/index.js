import styles from './Footer.module.css';

function Rodape() {
    return (
        <footer className={styles.footer}>
            <h2>Desenvolvido por Matheus Trojan</h2>
        </footer>
    )
}

export default Rodape;